<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:id="@+id/toolBar"
    android:layout_height="55dp"
    android:gravity="center_vertical"
    android:background="@color/white"
    android:weightSum="1">

    <ImageView
        android:id="@+id/toolBarBackIcon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="10dp"
        android:padding="8dp"
        android:src="@drawable/back_arrow"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/g_blue_gray200" />

    <TextView
        android:id="@+id/tootlBarTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="Welcome to Lesso"
        android:textColor="@color/g_blue_gray200"
        android:textSize="22sp"
        app:layout_constraintBottom_toBottomOf="@+id/toolBarBackIcon"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.50"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/toolBarBackIcon" />

</androidx.constraintlayout.widget.ConstraintLayout>